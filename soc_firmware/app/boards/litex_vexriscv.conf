# LiteX VexRiscv board-specific Kconfig
# Software PRNG (no hardware RNG on VexRiscv)
CONFIG_ENTROPY_GENERATOR=y
CONFIG_TEST_RANDOM_GENERATOR=y

# LiteX Ethernet driver
CONFIG_ETH_LITEX=y

# FPGA HAL — LiteX CSR backend
CONFIG_FPGA_HAL=y
CONFIG_FPGA_HAL_LITEX=y
